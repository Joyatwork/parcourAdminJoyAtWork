import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";

export function BenchmarkCharts() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Benchmarks Sectoriels (Anonymisés)</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Graphiques comparatifs */}
      </CardContent>
    </Card>
  );
}